# QUESTION NO: 246

### 한 회사가 Amazon Elastic Block Store (Amazon EBS)가 지원하는 Amazon EC2
인스턴스에서 애플리케이션을 실행합니다. 인스턴스는 매일 12 시간 동안 사용할 수 있어야 합니다. 회사는 애플리케이션에 필요한 창 밖에서는 인스턴스를 사용할 수 없도록 하여 비용을 절감하려고 합니다. 그러나 인스턴스를 사용할 수 없을 때마다 인스턴스의 메모리 내용을 보존해야 합니다. 솔루션 아키텍트가 이 요구 사항을 충족하려면 어떻게 해야 합니까?

**A. 애플리케이션의 가용성 창 밖에서 인스턴스를 중지합니다. 필요한 경우 인스턴스를 다시 시작하십시오.**

**B. 애플리케이션의 가용성 창 밖에있는 Hibernate 타이 인스턴스. 필요한 경우 인스턴스를 다시 시작하십시오.**

**C. Auto Scaling을 사용하여 애플리케이션의 가용성 창 밖에서 인스턴스를 축소합니다. 필요한 경우 인스턴스를 확장합니다.**

**D. 애플리케이션의 가용성 창 밖에서 인스턴스 종료 필요한 경우 사전 구성된 Amazon 머신 이미지  (AMI)를 사용하여 인스턴스 시작**

- 답 : `B`
- 오답 정리

    문제에 가용성 창이 나오는데 문제를 푸는 것과 아무 연관 없어서 일단 무시해도 될 듯..

    인스턴스를 사용할 수 없는 동안 메모리 내용을 보존하고 비용을 절감하는 것이 이 문제의 키워드이다.

    이를 만족하는 인스턴스는 최대 절전 모드 인스턴스이다. 따라서 답은 B

- 서비스 정리

    [**Availability Window](https://faq.calendarhero.com/article/202-how-do-i-customize-my-availability)** (가용성 창) : 뭔가.. 스케줄링 지정하는 창인듯..?

    ![https://s3-us-west-2.amazonaws.com/secure.notion-static.com/242c0117-b13c-446b-a448-b66ceeec8193/Untitled.png](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/242c0117-b13c-446b-a448-b66ceeec8193/Untitled.png)

    **Hibernate** : 최대 절전 모드, 인스턴스를 사용할 수 없을 때마다 인스턴스의 메모리 내용을 보존합니다. 인스턴스가 최대 절전 모드에 있는 동안에는 연결된 EBS 볼륨 및 Elastic IP 주소에 대해서만 요금을 지불하고 중지된 다른 인스턴스와 마찬가지로 다른 시간당 요금은 부과되지 않습니다.

    [관련 URL](https://docs.aws.amazon.com/ko_kr/AWSEC2/latest/UserGuide/Hibernate.html)

# QUESTION NO: 247

### 회사에서 미디어 공유 애플리케이션을 구축 중이며 Amazon S3를 스토리지로 사용하기로 결정했습니다. 미디어 파일이 업로드되면 회사는 썸네일을 생성하고 이미지의 객체를 식별하며 비디오를 표준 형식 및 해상도로 트랜스 코딩하고 메타 데이터를 추출하여 Amazon DynamoDB 테이블에 저장하기 위해 다단계 프로세스를 시작합니다. 메타 데이터는 검색 및 탐색에 사용됩니다. 트래픽의 양은 가변적 임 솔루션은 불필요한 비용없이 로드 급증을 처리하도록 확장 할 수 있어야 합니다.
솔루션 설계자가 이 워크로드를 지원하기 위해 권장해야 하는 것은 무엇입니까?

**A. Amazon S3에 콘텐츠를 업로드하는 데 사용되는 웹 사이트 또는 모바일 앱으로 처리를 구축합니다. 객체가 업로드 될 때 필요한 데이터를 DynamoDB 테이블에 저장합니다.**

**B. 객체가 S3 버킷에 저장 될 때 AWS Step Functions 트리거 Step Functions에서 객체를 처리하는 데 필요한 단계를 수행한 다음 메타 데이터를 DynamoDB 테이블에 씁니다.**

**C. 객체가 S3 버킷에 저장된 경우 AWS Lambda 함수 트리거 Lambda 함수가 AWS Batch를 시작하여 객체 처리 단계를 수행하도록 합니다. 완료되면 객체 데이터를 DynamoDB 테이블에 배치합니다**

**D. 객체가 Amazon S3에 업로드 될 때 DynamoDB 테이블에 초기 항목을 저장하도록 AWS Lambda 함수를 트리거합니다. Auto Scaling 그룹의 Amazon EC2 인스턴스에서 실행중인 프로그램을 사용하여 처리되지 않은 인덱스를 폴링하여 처리를 수행하는 프로그램 사용**

- 답 : `C? B?` 사용 사례에 명확히 명시되어있으므로 제 생각엔 B
- 오답 정리

    A - 트래픽이 가변적이라 하였으므로 탈락. 불필요한 비용이 추가적으로 들어감

    D - EC2를 사용하므로 비용 효율적이지 못하다.

- 서비스 정리

    **[Step Functions](https://docs.aws.amazon.com/ko_kr/step-functions/latest/dg/welcome.html)** : AWS Lambda 함수와 기타 서비스를 결합하여 비즈니스에 중요한 애플리케이션을 구축할 수 있는 서버리스 오케스트레이션 AWS 서비스. [URL](https://aws.amazon.com/ko/step-functions/?step-functions.sort-by=item.additionalFields.postDateTime&step-functions.sort-order=desc)

    업로드 후 즉시 데이터를 처리하기 위해 AWS 람다를 트리거하는 Amazon S3를 사용할 수 있다. 예를 들어, 람다를 사용하여 미리 보기 이미지, 트랜스코딩 비디오, 인덱스 파일, 프로세스 로그, 컨텐츠 검증, 데이터 집계 및 필터링 등을 실시간으로 수행할 수 있다.

    ![https://s3-us-west-2.amazonaws.com/secure.notion-static.com/3a7c7b88-a683-42d2-a508-a0f586cf9067/Untitled.png](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/3a7c7b88-a683-42d2-a508-a0f586cf9067/Untitled.png)

    **[AWS Batch](https://docs.aws.amazon.com/ko_kr/batch/latest/userguide/what-is-batch.html)** : AWS Batch 를 사용하면 AWS 클라우드에서 배치 컴퓨팅 워크로드를 실행할 수 있습니다. 배치 컴퓨팅은 개발자, 과학자 및 엔지니어가 대량의 컴퓨팅 리소스에 액세스할 수 있는 일반적인 방법입니다. 기존 배치 컴퓨팅 소프트웨어와 마찬가지로 필요한 인프라를 구성하고 관리하는 획일적인 과중한 작업을 AWS Batch 제거합니다. 이 서비스는 제출된 작업에 응답하여 리소스를 효율적으로 프로비저닝함으로써 용량 제한을 해소하고, 컴퓨팅 비용을 줄이며, 결과를 신속하게 제공할 수 있습니다.

    ![https://s3-us-west-2.amazonaws.com/secure.notion-static.com/793e6feb-1b5f-486b-8bff-bcbfcd616098/Untitled.png](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/793e6feb-1b5f-486b-8bff-bcbfcd616098/Untitled.png)
