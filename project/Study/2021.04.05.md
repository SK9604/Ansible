2021-04-07 시험 예정
# 1

**-Route 53이 DNS 쿼리에 무작위로 선택된 최대 8개의 정상 레코드로 응답하게 하려는 경우에 사용.**
**Answer: 이거 답 C 임** 

-트래픽 흐름에서 지원되는 고급 쿼리 유형 

**A : 지연 시간 라우팅 정책** : 라우트 53의 새 기능으로 전 세계에 있는 다른 엣지를 검색하여 최종 사용자에게 지연 시간이 가장 낮은 AWS 리전으로 라우팅한다.→ 성능 개선

**B : 가중치 기반 라우팅 정책 :** Weighted Round Robin, 가중치 기반 정책에서 응답이 처리되는 빈도를 지정하기 위해 리소스 레코드 세트에 가중치 할당. 예로 들어 하나의 DNS 이름과 연결된 두 개의 레코드 세트 중 하나에는 가중치 3, 다른 하나는 가중치 1을 부여. 가중치는 0~255사이의 숫자로 지정 가능.

**C : 지리적 위치 라우팅 정책 : 지역 DNS를 사용하는 것, 사용자의 위치에 기반하여 트래픽 라우팅하려는 경우에 사용합니다.** 지역 DNS를 사용하면 그 지역 기반의 언어로 된 세부 정보 페이지 표시, 라이선스가 있는 마켓으로 콘텐츠를 배포하거나 지역화된 콘텐츠를 사용자 지정할 수 있음. **지역 DNS는 대륙, 국가/지역 주라는 세 가지 수준의 지역 단위 제공 - 사용자 위치에 기반!! 이 포인트입니다**

- 지리 근접 라우팅 정책 : 리소스의 위치를 기반으로 사용자와 가장 인접한 서비스 리전으로 라우팅 합니다!!!!!

둘이 약간 다르니 차이점을 알아주세요~~~~~~~~

**D는 정확히 어떤 정책?**

다중 응답 라우팅을 사용하면 Amazon Route 53이 **DNS 쿼리에 대해 다수의 값(예: 웹 서버의 IP 주소)을 반환하도록 구성**할 수 있습니다. 다중 값은 거의 모든 레코드에 대해 지정할 수 있지만, 다중 응답 라우팅을 사용하면 각 리소스의 상태를 확인할 수도 있으므로 Route 53은 정상 리소스의 값만 반환합니다. 이것이 로드 밸런서를 대체하는 것은 아니지만, 다수의 상태 확인 가능한 IP 주소를 반환하는 기능은 DNS를 사용하여 **가용성 및 로드 밸런싱을 개선하는 한 방법**입니다. Route 53은 최대 8개의 정상 레코드로 DNS 해석기마다 다른 응답을 제공합니다

—저희 리눅스 수업에 dig, nslookup [www.daum.net](http://www.daum.net) 할 때 혹시 여러 IP 나오신거 기억하시는지...

그걸 말합니다! **최대 8개까지 등록 가능합니다. !!!!!** 

# 2

Answer: A

**Lustre 용 Amazon FSx  : 고성능 파일 시스템이며 빠르고 빠른 스토리지가 필요한 애플리케이션을 위해 설계됨.**

계속 증가하는 데이터 세트를 빠르고 저렴하게 처리, 스토리지 속도가 중요한 모든 워크로드에 사용 가능.

**Amazon FSx는 S3와 통합되어 S3 객체를 파일로 표시하고 변경된 데이터를 다시 S3에 쓸 수 있도록 함.**

**Q : Amazon FSx for Luster는 어떤 사용 사례를 지원합니까?**

A : 기계 학습, [고성능 컴퓨팅 (HPC)](https://aws.amazon.com/hpc/) , 비디오 처리, 재무 모델링, 게놈 시퀀싱 및 전자 설계 자동화 (EDA) 와 같이 속도가 중요한 워크로드에 Amazon FSx for Luster를 사용하십시오 .

오답 정리

**B : Windows 파일 서버용** Amazon FSx : indows 파일 서버용 Amazon FSx는 업계 표준 서비스 메시지 블록 (SMB) 프로토콜을 통해 액세스 할 수있는 완전 관리 형의 매우 안정적이고 확장 가능한 파일 스토리지를 제공합니다. 

- **Q : 내 파일 시스템에 액세스 할 수 있는 인스턴스 유형 및 OS 버전은 무엇입니까?**

**C : EBS와 S3 Glacier  -** 수명이 짧은 사용을 위하기엔 S3 Glacier은 적절하지 않다. 또한, 데이터 세트 및 출력 파일을 읽고 쓰기 위해 영구 저장소와 통합 된 고성능 파일 시스템을 요구하므로 EBS와는 맞지 않다.

**D : 범용 SSD (gp2) 볼륨과 통합된 EBS와 엔드포인트가 있는 S3 버킷 :**  EBS에는 2개의 큰 카테고리가 있음

1. SSD 지원 스토리지 - **트랜잭션** 워크로드 ( 주로 IOPS, 지연 시간 및 내구성이 성능을 좌우 ) SSD 지원 볼륨은 프로비저닝된 IOPS SSD ( io1, io2)와 범용 SSD (gp3, gp2)  → 트랜잭션 워크로드 [https://terms.naver.com/entry.nhn?docId=1180131&cid=40942&categoryId=32843](https://terms.naver.com/entry.nhn?docId=1180131&cid=40942&categoryId=32843)
2. HDD 지원 스토리지 - 처리량 워크로드 ( MB/초로 측정한 **처리량이 성능을 좌우** ) HDD 지원 볼륨은 처리량 최적화 HDD(st1)와 콜드 HDD(sc1)를 포함합니다.

-D는 SSD가 아닌 HDD를 사용해야 적절할 것 같다.

- EBS 볼륨 유형(중요!)
