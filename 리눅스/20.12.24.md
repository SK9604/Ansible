## 🙋🏻‍♀️ 스물세째 비대면 수업

---

# 내용 정리

---

# 부팅과정

1. 시스템 전원을 켭니다. 시스템 펌웨어(최신 UEFI 또는 구식 BIOS)는 POST( Power On Self Test )를 실행하고 하드웨어 일부를 초기화하기 시작합니다.
시스템 BIOS/UEFI 구성 화면을 사용하여 구성하며, 이 화면은 일반적으로 부팅 절차 중 특정 키 조합을 눌러 도달합니다(예: 부팅 절차가 시작되면 **F2**를 빨리 누름).
2. 시스템 펌웨어가 UEFI 부트 펌웨어에 구성된 부팅 가능 매체를 검색하거나 모든 디스크에서 MBR(Master Boot Record )을 BIOS에 구성된 순서로 검색합니다.
시스템 BIOS/UEFI 구성 화면을 사용하여 구성하며, 이 화면은 일반적으로 부팅 절차 중 특정 키 조합을 눌러 도달합니다(예: 부팅 절차가 시작되면 **F2**를 빨리 누름).
3. 시스템 펌웨어가 디스크에서 부트 로더 를 읽은 다음 시스템 컨트롤을 부트 로더로 전달합니다.
Red Hat Enterprise Linux 7 시스템에서 이 컨트롤은 일반적으로 **grub2**입니다.
**grub2-install**을 사용하여 구성합니다.
4. 부트 로더는 디스크에서 구성을 로드하고 사용자에게 부팅이 가능한 구성 메뉴를 표시합니다.
**/etc/grub.d/**, **/etc/default/grub**, **/boot/grub2/grub.cfg**(수동이 아님)를 사용하여 구성합니다.
5. 사용자가 선택을 하면(또는 자동 시간 제한이 발생할 경우) 부트 로더가 디스크에서 구성된 커널과 initramfs 를 로드하여 메모리에 배치합니다. **initramfs**는 부팅, init 스크립트 등에 필요한 모든 하드웨어에 대한 커널 모듈이 포함된 **gzip**-ed **cpio** 아카이브입니다. Red Hat Enterprise Linux 7에서 **initramfs**에는 자체적으로 사용 가능한 전체 시스템이 포함되어 있습니다.
**/etc/dracut.conf**을 사용하여 구성합니다.
6. 부트 로더는 시스템 컨트롤을 커널로 전달하여 부트 로더의 커널 명령줄에 지정된 옵션과 메모리의 **initramfs** 위치를 전달합니다.
**/etc/grub.d/**, **/etc/default/grub**, **/boot/grub2/grub.cfg**(수동이 아님)를 사용하여 구성합니다.
7. 커널은 **initramfs**에서 드라이버를 찾을 수 있는 모든 하드웨어를 초기화한 다음 **initramfs**에서 **PID 1**로 **/sbin/init**을 실행합니다. Red Hat Enterprise Linux 7에서 **initramfs**에는 **/sbin/init**로 **systemd**의 작업 복사본과 **udev** 데몬이 포함되어 있습니다.
**init=** 명령줄 매개 변수를 사용하여 구성 
8. **initramfs**의 **systemd** 인스턴스는 **initrd.target** 대상에 대한 모든 유닛을 실행합니다. 여기에는 **/sysroot**에 실제 루트 파일 시스템 마운팅이 포함됩니다.
**/etc/fstab**을 사용하여 구성합니다.
9. 커널 루트 파일 시스템이 **initramfs** 루트 파일 시스템에서 **/sysroot**의 이전에 마운트된 시스템 루트 파일 시스템으로 전환(피벗)됩니다. 
그런 다음 **systemd**에서 시스템에 설치된 **systemd**의 복사본을 사용하여 자체적으로 다시 실행합니다.
10. **systemd** 는 커널 명령줄에서 전달되거나 시스템에 구성된 기본 대상을 찾은 다음 해당 대상에 대한 구성을 준수할 유닛을 시작(및 중지)하여 유닛 간의 종속성을 자동으로 해결합니다. 즉, **systemd** 대상은 원하는 시스템 상태에 도달하기 위해 활성화해야 하는 유닛의 집합입니다 . 이러한 대상에는 일반적으로 최소한 텍스트 기반 로그인 또는 그래픽으로 생성된 로그인 화면이 포함됩니다.
**/etc/systemd/system/default.target**, **/etc/systemd/system/**을 사용하여 구성합니다.

### 컴퓨터의 기본 부팅 과정

전원 - POST - BIOS - Grub - kernel - system ON

실제 RHEL 7,8 버전에서는 어떠한 부팅순서를 가지고 파일은 어떻게 되어 있으며 예전에 사용되던 파일은 어떻게 되어있고 현재 디렉토리 구조는 어찌 되어있고 프로세스는 무엇을 사용하고 있으며 최종단계에 오기까지 사용되는 또 다른 파일은 무엇이 있는지 확인

부팅이 완료되었다

사용자 : 로그인이 가능하다

프로세스 : systemd가 실행완료

## 관리자 비밀번호 변경

```bash
# echo $RANDOM | passwd --stdin root
# reboot
```

서버가 부팅되기 까지의 시간. 작업시간. 재설정. 부팅완료

서버는 재부팅이 되어있고 비밀번호는 모르는 상태고 서버는 운영중이다.

poweron - 클릭 > 방향키 조정 > e 누르기 

과거 서버의 메모리를 사용하면서 용량이 적은 경우 서버의 사용량이 많은 경우

프로세스가 되기 위해서는 메모리에 상주

# 단원목표

---

- SWAP 이란?
- SWAP 추가 / 삭제

---

# 스왑(SWAP)?

 

```bash
--+-------------+---
 	|             |
	|             |
	|    ext4     |     <---- # df -h   (# df -h -T)
	|             |
	|             |
	+-------------+
	|   minfree   |
--+-------------+---
	|   swapfs    |     <---- # cat /proc/swaps (# swapon -s)
--+-------------+---
```

(1) 스왑(SWAP)이란?

```bash
MEM(RAM)                   DISK
	+--------+---------+        +--------------+
	|        |         |        |              |
	|        |         |        |  File System |  <---- # df -h
	|        |         |        |     (ext4)   |
	|        |         |        |              |
	+--------+---------+        +--------------+
	|        |         |        |              |
	|        |         |        |     SWAP     |  <---- # cat /proc/swaps (# swapon -s)
	|        |         |        |              |
	|        |         |        |              |
	+--------+---------+        +--------------+
```

[참고] 페이징(Paging) 발생

**Paging 증가 -> MEM(RAM) 부족 -> 성능 떨어짐 -> MEM 증설 고려**

SWAP? 디스크내에 존재하는 가상적인 메모리 공간, 물리적인 메모리(RAM) 연장 공간처럼 쓰이는 공간

스왑(SWAP)은 메모리 내의 데이터 페이지 또는 세그먼트를 교체하는 것이다. 스왑핑은 컴퓨터가 주기억장치보다 더 큰 프로그램이나 데이터 파일을 다룰 수 있게 하는 유용한 테크닉이다. 운영체계는 주기억장치 내에 가능한 한 많은 데이터를 복사해 넣고, 나머지는 디스크 상에 남겨둔다. 운영체계가 디스크에 있는 데이터를 필요로할 때, 주기억장치 내의 데이터 일부를 디스크 상의 데이터와 교체한다. DOS는 스왑핑을 수행하지 않지만, OS/2, 윈도우, 유닉스 등을 포함한 많은 운영체계들이 스왑핑을 수행한다. 스왑핑은 때로, 페이징이라고 불리기도 한다.

![https://s3-us-west-2.amazonaws.com/secure.notion-static.com/e3f5b51c-0751-4271-a726-c359a0a3dca5/_2020-04-06__10.48.00.png](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/e3f5b51c-0751-4271-a726-c359a0a3dca5/_2020-04-06__10.48.00.png)

■ 스왑 관련 이슈(SWAP Issue)?

- **언제 스왑을 추가하는가?**

- **스왑 공간의 크기?**

**(초기 설치시)**

**(운영시)**

- **스왑을 추가하면 성능이 좋아지는가? > 아니다**

메모리가 부족하다 → 가상메모리를 사용

디스크의 I/O에 영향, CPU를 소모, 버스 점유 → 전체 성능 저하

메모리를 많이 연결, CPU 처리해야 되는 버스

메모리가 부족할 때 미리 준비해두고 바로 사용할 수 있는 상태로 준비

■ 스왑 공간 크기에 대한 권장사항

```bash
--------------------------------------------
RAM SIZE			SWAP SIZE
--------------------------------------------
 4GB 이하이면		minimum  2GB
 4GB ~ 16GB		  minimum  4GB
16GB ~ 64GB		  minimum  8GB
64GB ~ 256GB		minimum 16GB
256G ~ 512GB		minimum 32GB
--------------------------------------------
```

# 스왑(SWAP) 관리

```bash
■ SWAP 추가 방법
	- SWAP File(EX: /swap/swapfile)
		(ㄱ) 큰 파일 생성
	  	   # dd if=/dev/zero of=/swap/swapfile bs=1M count=10240 
		(ㄴ) 스왑 파일시스템 생성(swapfs)
		     # mkswap /swap/swapfile 
		(ㄷ) 스왑 Activate(swap on)
		     # swapon /swap/swapfile
		(ㄹ) /etc/fstab 파일 설정
		     # vi /etc/fstab
	- SWAP Partition(EX: /dev/sdb1)
		(ㄱ) 파티션 준비
		     # fdisk /dev/sdb (System Partition ID : 82)
		(ㄴ) 스왑 파일시스템 생성(swapfs)
		     # mkswap /dev/sdb1
		(ㄷ) 스왑 ON(swap on)
		     # swapon /dev/sdb1
		(ㄹ) /etc/fstab 파일 설정
		     # vi /etc/fstab
```

(1) 스왑 확인 방법

현재 스왑 공간(스왑 공간, 스왑 전체크기, 스왑사용량)을 확인하는 방법은

(ㄱ) free 명령어를 사용하는 방법이나

(ㄴ) top 명령어를 통해 확인하는 방법

(ㄷ) /proc/swaps 파일을 확인하는 방법

(ㄹ) /proc/meminfo 파일을 확인하는 방법

등이 있다.

스왑을 추가하고 확인하거나 스왑을 삭제하고 확인할 때는 /proc/swaps 또는 free 명령어를 사용하는 방법이 적당하다. 스왑 사용량을 모니터링 할 때는 top 명령어를 사용하는 것이 좋다.

### [EX] 스왑 공간/스왑 용량/스왑 사용량 확인

```bash
# cat /proc/swaps   (# swapon -s)
Filename				Type		  Size	  Used	Priority
/dev/sda1       partition	2097148	0	    -2

# free
              total        used        free      shared  buff/cache   available
Mem:         964048      404332      164360        7144      395356      368812
Swap:       4194300           0     4194300
# free -h
              total        used        free      shared  buff/cache   available
Mem:          941Mi       395Mi       160Mi       6.0Mi       386Mi       359Mi
Swap:         4.0Gi          0B       4.0Gi

# top | head -5

top - 11:40:00 up 59 min,  1 user,  load average: 0.01, 0.02, 0.05
Tasks: 204 total,   1 running, 203 sleeping,   0 stopped,   0 zombie
%Cpu(s):  3.0 us,  3.0 sy,  0.0 ni, 93.9 id,  0.0 wa,  0.0 hi,  0.0 si,  0.0 st
KiB Mem :  4026236 total,  3199044 free,   473596 used,   353596 buff/cache
**KiB Swap:  2097148 total,  2097148 free,        0 used.  3314808 avail Mem**

# cat /proc/meminfo 
MemTotal:        4026236 kB
MemFree:         3199916 kB
MemAvailable:    3315724 kB
Buffers:            2096 kB
Cached:           315620 kB
SwapCached:            0 kB
Active:           322024 kB
Inactive:         264180 kB
Active(anon):     269756 kB
Inactive(anon):    13588 kB
Active(file):      52268 kB
Inactive(file):   250592 kB
Unevictable:           0 kB
Mlocked:               0 kB
**SwapTotal:       2097148 kB**  <- 전체 스왑 메모리 용량
**SwapFree:        2097148 kB**  -< 사용가능한 스왑 메모리 용량
Dirty:                 0 kB
Writeback:             0 kB
AnonPages:        268564 kB
Mapped:            99076 kB
Shmem:             14856 kB
Slab:              93684 kB
SReclaimable:      35924 kB
SUnreclaim:        57760 kB
KernelStack:        7632 kB
PageTables:        20028 kB
NFS_Unstable:          0 kB
Bounce:                0 kB
WritebackTmp:          0 kB
CommitLimit:     4110264 kB
Committed_AS:    2149228 kB
VmallocTotal:   34359738367 kB
VmallocUsed:      219884 kB
VmallocChunk:   34359277564 kB
HardwareCorrupted:     0 kB
AnonHugePages:     63488 kB
CmaTotal:              0 kB
CmaFree:               0 kB
HugePages_Total:       0
HugePages_Free:        0
HugePages_Rsvd:        0
HugePages_Surp:        0
Hugepagesize:       2048 kB
DirectMap4k:      126784 kB
DirectMap2M:     3018752 kB
DirectMap1G:     3145728 kB

[참고] 스왑의 우선순위(Priority)
# cat /proc/swaps    (# swapon -s)
Filename                                Type            Size    Used    Priority
/dev/sda2                               partition       1052248 0       -1

# man swapon
       -p 우선권
              swapon 에서 사용될 우선권을 설정한다.  이 옵션은 swapon 이  커 널
              1.3.2   와 그 이후 버전에서 컴파일된 경우에만 가능하다.  priority
              은 0 ~ 32767 사이의 값이다.  스왑 우선권에 대한 자세한   내 용 은
              swapon(2)  을 참고하라.  pri=value 이라는 항목을 /etc/fstab 의 옵
              션 필드에 써넣으면 swapon -a 할 때 이용~~된~~다.

# man 2 swapon
PRIORITY
       각각의 스왑 영역은 높던 낮던 우선권을 가지고 있다.  디폴트는 '낮음'이다.
       낮은 우선권 영역에서새 영역은 이전 영역보다 낮은 우선권을 갖는다.

       swapflags 으로 설정된 모든 우선권은 상위 우선권이다. 그들은 호출자에  의
       해 골라진 음수가 아닌 값을 갖는다.  높은 번호는 높은 우선권을 말한다.

       스 왑 페이지는 우선권 순서대로 영역으로부터 할당된다, 제일 높은 우선권이
       처음.  서로 다른 우선권을 가지고 있기 때문에, 낮은 우선권 영역을 쓰기 전
       에 높은 우선권 영역은 고갈된다.  두개 이상의 영역이 같은 우선권을 가지고
       있고, 제일 높은 우선권이 가능하다면, 페이지는 그들 사 이 의  round-robin
       basis에 할당된다.
```

(2) 스왑(SWAP) 추가 방법

스왑은 파일 형태와 파티션 형태로 추가가 가능하며, 스왑을 현재 추가 할때는 mkswap, swapon 명령어를 사용하고 부팅시에 스왑을 마운트 할 수 있도록 하기 위해서는 /etc/fstab 파일을 사용한다. 스왑 관리에 관련된 명령어는 dd, mkswap, swapon, swapoff, rm 명령어등이 있다.

```bash
■ SWAP 추가 방법
	- SWAP File(EX: /swap/swapfile)
		# dd if=/dev/zero of=/swap/swapfile bs=1M count=10240
		# mkswap /swap/swapfile
		# swapon /swap/swapfile
		# vi /etc/fstab

	- SWAP Partition(EX: /dev/sdb1)
		# fdisk /dev/sdb (System ID : 82)
		# mkswap /dev/sdb1
		# swapon /dev/sdb1
		# vi /etc/fstab
```

### [EX] 스왑 파일 추가 실습

```bash
① 스왑 공간 확인
# free 
            total        used        free         shared     buff/cache   available
Mem:        4026236      473044     3199412       14856      353780       3315360
Swap:       2097148           0     2097148

# cat /proc/swaps    (# swapon -s)
Filename				  Type		  Size	  Used	Priority
/dev/sda1         partition	2097148	0   	-2

② 스왑 파일 생성
# mkdir /swap 
# dd if=/dev/zero of=/swap/swapfile bs=1024 count=102400
102400+0 records in
102400+0 records out
104857600 bytes (105 MB, 100 MiB) copied, 0.754211 s, 139 MB/s
# dd if=/dev/zero of=/swap/swapfile1 bs=1M count=1024
1024+0 records in
1024+0 records out
1073741824 bytes (1.1 GB, 1.0 GiB) copied, 1.25003 s, 859 MB/s
-> (if: input file, of: output file, bs: block size, count: block count)
-> dd명령을 이용해서 swapfile이라는 스왑 파일을 만든다.
-> /dev/zero 포맷으로 /swapdir/swapfile 파일에 대해서 블럭 사이즈는 1024bytes(=sector) 단위로 102400(100M)
   개 만큼 채운다.

③ 스왑 영역 지정 및 스왑 활성화(SWAP ON)
# mkswap /swap/swapfile 
Setting up swapspace version 1, size = 104853 kB

	[참고] 스왑 파일의 위치는 절대경로/상대경로 모두 지정이 가능
	# mkswap /swap/swapfile
	or
	# cd /swap
	# mkswap swapfile
# file swapfile
swapfile: data
# mkswap swapfile
mkswap: swapfile: insecure permissions 0644, 0600 suggested.
Setting up swapspace version 1, size = 100 MiB (104853504 bytes)
no label, UUID=9f2a5c3b-a44e-497e-9e6a-bbcabfa9e207
# file swapfile
swapfile: Linux/i386 swap file (new style), version 1 (4K pages), size 25599 pages, no label, UUID=9f2a5c3b-a44e-497e-9e6a-bbcabfa9e207

# swapon /swap/swapfile 
swapon: /swap/swapfile: insecure permissions 0644, 0600 suggested.
권한 설정변경후 다시 등록

# ls -l /swap/swapfile 
-rw-r--r--. 1 root root 104857600  4월  6 11:53 /swap/swapfile
# chmod 600 /swap/swapfile
# swapon /swap/swapfile
swapon: /swap/swapfile: swapon failed: 장치나 자원이 동작
# swapoff /swap/swapfile 
# swapon /swap/swapfile

# cat /proc/swaps   (# swapon -s)
Filename				Type		  Size	  Used	Priority
/dev/sda1       partition	2097148	0	    -2
/swap/swapfile  file		  102396	0	    -3

# swapoff swapfile
# swapon -p 10 swapfile
# swapon -s
Filename				  Type		  Size	  Used	Priority
/dev/sda5         partition	4194300	13156	-2
/swap/swapfile    file    	102396	0	    10

# free
              total        used        free      shared  buff/cache   available
Mem:        4026236      518044     2945904       14860      562288     3255068
Swap:       2199544           0     2199544

④ /etc/fstab 파일에 등록
# vi /etc/fstab 
..... (중략) .....
LABEL=SWAP-sda2         swap                    swap    defaults        0 0
..... (중략) .....
#
# SWAP Configuration
#
/swap/swapfile          swap                    swap    defaults        0 0  <----- 마지막 라인에 추가

-> 부팅시에 swap이 추가 될수 있도록 /etc/fstab 파일의 마지막 라인에 추가한다.
```

(3) 스왑 삭제 방법

```bash
■ SWAP 추가 방법
	- SWAP File(EX: /swap/swapfile)
		# dd if=/dev/zero of=/swap/swapfile bs=1M count=10240
		# mkswap /swap/swapfile
		# swapon /swap/swapfile
		# vi /etc/fstab

		# swapoff /swap/swapfile 
		# vi /etc/fstab 
	- SWAP Partition(EX: /dev/sdb1)
		# fdisk /dev/sdb (System ID : 82)
		# mkswap /dev/sdb1
		# swapon /dev/sdb1
		# vi /etc/fstab

		# swapoff /dev/sdb1
		# vi /etc/fstab
```

[EX] 스왑(SWAP) 파일 삭제

```bash
① 스왑 비활성화 및 확인(SWAP OFF)
# cat /proc/swaps
Filename				Type		  Size	  Used	Priority
/dev/sda1       partition	2097148	0	    -2
/swap/swapfile  file		  102396	0	    -3

# swapoff /swap/swapfile 
# cat /proc/swaps          (# swapon -s)
Filename                                Type            Size    Used    Priority
/dev/sda2                               partition       1052248 0       -1

# free                     (# free -m : MegaBytes)
            total        used        free         shared     buff/cache   available
Mem:        4026236      473044     3199412       14856      353780       3315360
Swap:       2097148           0     2097148

② 스왑 파일 삭제
# ls -lh /swap 
합계 100M
-rw-------. 1 root root 100M  4월  6 11:53 swapfile

# rm /swap/swapfile 
rm: remove 일반 파일 `/swap/swapfile'? y
-> /swap/swapfile을 SWAP ON 되었을때 자동으로 삭제되는 파일이 아니다. 따라서 별도로 삭제 해 줘야 한다.
-> 스왑으로 사용되고 있던 파일이 SWAP OFF 되면 스왑파일(예: /swap/swapfile)로 사용되던 파일은 불 필요한 파일이 된다.
```

[EX] 추가적인 실습(스왑 파티션 추가(EX: /dev/sdb1))

```bash
(전제조건) /dev/sdb 디스크가 사용중이면 안된다.
		# df –h 
		-> /dev/sdb1(/testmount)
DISK sdb 추가
4G
우선순위 10
부팅시에 자동으로 사용되도록 하기
```

```bash
① /dev/sdb 디스크의 system partition id 변경(83 -> 82)
Command (m for help): t
# fdisk –l /dev/sdb 
Disk /dev/sdb: 2147 MB, 2147483648 bytes, 4194304 sectors
Units = sectors of 1 * 512 = 512 bytes
Sector size (logical/physical): 512 bytes / 512 bytes
I/O size (minimum/optimal): 512 bytes / 512 bytes
Disk label type: dos
Disk identifier: 0x9b135594

   Device Boot      Start         End      Blocks   Id  System
  /dev/sdb1         2048     4194303     2096128   82  Linux swap / Solaris

# fdisk /dev/sdb 
-> /dev/sdb1(System Partition ID : 82)

② swapfs 생성
# mkswap /dev/sdb1 
Setting up swapspace version 1, size = 2096124 KiB
no label, UUID=fabdea52-a775-42d5-9798-f5cef8cecb0c

③ SWAP 활성화 및 확인
# swapon /dev/sdb1 
# blkid
/dev/sda1: UUID="3dc251d4-934f-4a78-86cf-826719522bd5" TYPE="swap" 
/dev/sda2: UUID="e79db2f6-576a-4520-b08e-058d2cc78b72" TYPE="xfs" 
/dev/sda3: UUID="fdb96437-02f3-4adc-aa29-2d56aaadadf4" TYPE="xfs" 
/dev/sda5: UUID="4888dd97-7b2d-4e25-bbf0-0ed998e42ef7" TYPE="xfs" 
/dev/sda6: UUID="44f8c6ee-a854-454d-ac81-2e652a551800" TYPE="xfs" 
/dev/sda7: UUID="047649d6-943b-4074-a90a-e6811b916597" TYPE="xfs" 
/dev/sda8: UUID="d88bb13c-5202-4aa9-8f29-280b8a1c3a40" TYPE="xfs" 
**/dev/sdb1: UUID="fabdea52-a775-42d5-9798-f5cef8cecb0c" TYPE="swap"**

# swapoff /dev/sdb1
# swapon -p 10 /dev/sdb1

# cat /proc/swaps 
Filename				Type		  Size	  Used	Priority
/dev/sda1       partition	2097148	0	    -2
/dev/sdb1       partition	2096124	0	    -3

# free
              total        used        free      shared  buff/cache   available
Mem:        4026236      520752     3047312       14868      458172     3253612
Swap:       4193272           0     4193272

④ 부팅시의 설정
# vi /etc/fstab 
..... (중략) .....
#
# SWAP Configuration
#
/swap/swapfile          swap            swap    defaults        0 0
/dev/sdb1               swap            swap    defaults        0 0

⑤ swap 삭제
# cat /proc/swaps
Filename				Type		  Size	  Used	Priority
/dev/sda1       partition	2097148	0	    -2
/dev/sdb1       partition	2096124	0	    -3

# swapoff /dev/sdb1 
# cat /proc/swaps 
Filename				Type		  Size	  Used	Priority
/dev/sda1       partition	2097148	0	    -2

# free
              total        used        free      shared  buff/cache   available
Mem:        4026236      521344     3046724       14868      458168     3253020
Swap:       2097148           0     2097148

# vi /etc/fstab 
..... (중략) .....
#
# SWAP Configuration
#
#/swap/swapfile          swap            swap    defaults        0 0
#/dev/sdb1               swap            swap    defaults        0 0

# fdisk /dev/sdb 
/dev/sdb1(System Partition ID :83)
# mkfs.ext4 /dev/sdb1
```

[참고] SWAP 추가하는 경우

MEM 사용량 80% 이상 -> SWAP 공간 추가(File or Partition) -> 결정(EX: /swap/swapfile)

(정리) SWAP File 추가하는 방법

```bash
(ㄱ) 큰 파일 생성
		# mkdir /swap
		# dd if=/dev/zero of=/swap/swapfile bs=1024 count=102400
	(ㄴ) swapfs 파일시스템 생성
		# mkswap /swap/swapfile
	(ㄷ) SWAP ON
		# swapon /swap/swapfile
	(ㄹ) /etc/fstab 편집
		# vi /etc/fstab
		/swap/swapfile     swap      swap    defaults        0 0

       SWAP File 삭제하는 방법
	(ㄱ) SWAP OFF
		# swapoff /swap/swapfile
	(ㄴ) /etc/fstab 편집
		# vi /etc/fstab
	(ㄷ) 스왑 파일 삭제
		# rm /swap/swapfile

       SWAP 확인 방법
		# free
		# cat /proc/swaps
		# top
```

# 사용자 & 그룹관리

# 단원목표

---

- 사용자 계정
    - 사용자 정보 파일
    - 사용자 관리 명령어
        - **useradd**
        - **usermod**
        - **userdel**
- 그룹 계정
    - 그룹 정보 파일
    - 그룹 관리 명령어
        - **groupadd**
        - **groupmod**
        - **groupdel**
- 패스워드 에이징 (**password aging**)

---

사용자 : 

계정 : account 계좌, 신원증명, 시스템을 사용하는 것은 사람

컴퓨터 → 작업

시스템에 권한이 있는 사용자(사람)가 접근하여 특정 자원에 대하여 이용 할 수 있도록 부여된 권한대로 제어하기 위한 장치

그룹?: 큰 의미의 신원증명, 권한에 대하여 한번에 설정을 할 수 있는 특수권한

root , 실제 사람이 아닌 계정 → 사람을 위한 계정

은행 → 계좌 개설 → 신분증 → 신원증명 → 계좌 발급

test1 계정

server1.example.com 생성 [자원-리소스]

---

## 사용자 관리(User Administration)

### (1) 사용자 정보 파일

- /etc/passwd	사용자의 기본적인 정보가 저장된 파일
- /etc/shadow	사용자의 암호가 저장된 파일, 암호를 설정하기 위한 정책도 저장

```bash
# cat /etc/passwd
# cat /etc/shadow
# cat /etc/group
# ls -l /etc/skel/
# cat /etc/default/useradd
컴퓨터의 시간의 기준 1970.01.01 00:00:00
```

```bash
# cat /etc/passwd | grep root
root:x:0:0:root:/root:/bin/bash
```

(/etc/passwd 파일의 필드 해석) ( 필드 구분자 : )

```bash
# cat /etc/passwd | grep root | head -1
root:x:0:0:root:/root:/bin/bash

필드         설  명
root        사용자 이름. 계정을 의미
x           사용자 암호(보안상 /etc/shadow 파일에 암호를 옮겨 놓았음)
0           사용자 아이디(UID)
0           그룹 아이디(GID), 사용자의 주 그룹(Primary Group)
root        설명 정보(Comment)
/root       홈 디렉토리
/bin/bash   로그인 쉘
```

(/etc/shadow 파일의 필드 해석)

```bash
# cat /etc/shadow | head -1
root:$6$5N2OnjCj$rxt47kbaPTEtVBZCtNLc2aHQkfBYkxdXO2wAnuijX9Bf0AhFZlrdZwsKPP3R3WUlZifahQdBrmv0uOJtVjZTK/:18351:0:99999:7:::

필드         설  명
root        사용자 이름(login name)
$6$5N2Onj~  사용자 암호(encrypted password)
18351       암호변경일(days since Jan 1, 1970 that password was last changed)
0           암호를 변경할 수 없는 최소 날짜 (days before password may be changed)
99999       암호를 사용할 수 있는 최대 날짜 (days after which password must be changed)
7           사용자 암호 만료되기전 사용자에게 경고 메세지를 주는 기간 (days before password is to expire that user is warned)
            사용자 로그인하지 않으면 암호를 비활성화 시키는 기간 (days after password expires that account is disabled)
            사용자 암호 만료 기간, 절대로 넘을 수 없는 기간 (days since Jan 1, 1970 that account is disabled)
            아직 기능이 없음

암호필드 추가
$[숫자]$[salt]$[hash 결과]
HASH 종류
$1 : MD5
$2 : 
$5 : SHA-256
$6 : SHA-512
salt : 동일한 문자열로 암호를 저장하더라도 같은 암호화 값을 가지지 않도록 변경해주는 역할
hash 결과 : hash 종류와 salt 값으로 구성되어 있는 결과 값

◎ 같은 비밀번호여도 salt값에 의해 암호화값이 서로 다르다
# useradd test1 ; useradd test2
# echo redhat | passwd --stdin test1
Changing password for user test1.
passwd: all authentication tokens updated successfully.
# echo redhat | passwd --stdin test2
Changing password for user test2.
passwd: all authentication tokens updated successfully.
# cat /etc/shadow | tail -2
test1:$6$ylW4z9/1GjznpcD6$olLUskFUkSctqhtqRDks8ySCpW.CyA/kZWgxkjk4PiwqR86H9kVPU33Nlsp9t2RKks/7hK3wARcBxqSQFw6nX0:18620:0:99999:7:::
test2:$6$k2cwlmqC8FMFisyo$6nOmUwIGsQrefKr0ec67dls/ZeJHAp1DwtyJDofduWglywJRC7t/MR6ZOBv0x5hU4WXUVUubnk3JlUSYnsPL.1:18620:0:99999:7:::

pwunconv > /etc/shadow에 저장된 암호를 /etc/passwd로 이동
pwconv > /etc/passwd에 저장된 암호를 /etc/shadow로 이동
```

### (2) 사용자 관리 명령어

```bash
사용자 관리 명령어는 크게 3가지로 분류한다.
useradd CMD사용자 정보 추가 명령어
usermod CMD사용자 정보 변경 명령어
userdel CMD사용자 정보 삭제 명령어
```

## (2-1) useradd 명령어

```bash
useradd 명령어나 adduser 명령어 모두 수행 가능하다.

(명령어 형식)
# useradd [options] LOGIN
Options:
-b, --base-dir BASE_DIR  base directory for the new user account home directory
-c, --comment COMMENT  set the GECOS field for the new user account
-d, --home-dir HOME_DIR  home directory for the new user account
-D, --defaults  print or save modified default useradd configuration
-e, --expiredate EXPIRE_DATE  set account expiration date to EXPIRE_DATE
-f, --inactive INACTIVE  set password inactive after expiration to INACTIVE
-g, --gid GROUP  force use GROUP for the new user account
-G, --groups GROUPS  list of supplementary groups for the new user account
-h, --help  display this help message and exit
-k, --skel SKEL_DIR  specify an alternative skel directory
-K, --key KEY=VALUE  overrides /etc/login.defs defaults
-m, --create-home  create home directory for the new user account
-l,  do not add user to lastlog database file
-M,  do not create user's home directory(overrides /etc/login.defs)
-r,  create system account
-o, --non-unique  allow create user with duplicate(non-unique) UID
-p, --password PASSWORD  use encrypted password for the new user account
-s, --shell SHELL  the login shell for the new user account
-u, --uid UID  force use the UID for the new user account
-Z, --selinux-user SEUSER  use a specific SEUSER for the SELinux user mapping

(/etc/passwd)
user01:x:1500:1500:Test User:/home/user01:/bin/bash
          -u   -g    -c         -d           -s

(명령어 사용예)
# useradd -u 1300 -g users -c "Test User" -d /users/user01 -s /bin/ksh user01
# useradd -d /users/user01 user01

# useradd user01
# passwd user01

		■ useradd 명령어의 기본 옵션(Default Options)
		---------------------------------------------------------------------------------
		-u UID자동으로 UID 선택(UID 1000이상), 기존 사용자 UID 다음번째 UID 선택
		-g GID자동으로 GID 선택(GID 1000 이상), 사용자 이름과 동일한 그룹 생성
		-c COMMENT설명은 없음
		-d HOME_DIR/home/$USER 디렉토리 생성
		-s /bin/bash기본적으로 /bin/bash 지정
		---------------------------------------------------------------------------------
```

### [참고] : 계정을 생성할때 기초 파일은 어디서 생성되는가?

```bash
# cd /etc/skel
# ls -la
합계 24
drwxr-xr-x.   3 root root   78  3월 20 11:49 .
drwxr-xr-x. 140 root root 8192  3월 31 09:59 ..
-rw-r--r--.   1 root root   18  8월  8  2019 .bash_logout
-rw-r--r--.   1 root root  193  8월  8  2019 .bash_profile
-rw-r--r--.   1 root root  231  8월  8  2019 .bashrc
drwxr-xr-x.   4 root root   39  3월 20 11:48 .mozilla

계정 생성시 기본적으로 사용할수 있는 파일들을 저장하는 디렉토리이다.
생성 임시 테스트
# ls
# echo "skel dir TEST" > file1.txt
# ls
file1.txt
# pwd
/etc/skel
test1 계정을 생성하고 스위칭한다
# useradd test1
# passwd test1
# su - test1
[test1@server1 ~]$ ls
file1.txt
[test1@server1 ~]$ cat file1.txt 
skel dir TEST
[test1@server1 ~]$ ls -l
total 4
-rw-r--r--. 1 test1 test1 14 Mar 31 10:07 file1.txt
/etc/skel 디렉토리에 존재하는 파일들의 경우에는 계정을 생성하면서 여러가지 계정에 필요한 파일들을 자동 복사해준다. 파일의 권한은 생성되는 게정의 권한으로 전환된다.

SKEL에 필요한 디렉토리를 여러가지 만들어두고 필요한 경우 변경해서 사용하면 편하다.
EX : 관리자 계정용 SKEL , 일반 계정용 SKEL 
```

```bash
(전제 조건) 기존에 존재하는 사용자는 없어야 한다.
       user01/user02/user03 .....
       만약 사용자가 존재한다면 아래와 같이 삭제한다.
       # cat /etc/passwd | tail 
       # userdel -r <사용자이름>

① user01 사용자 추가 및 정보 확인

■ 사용자 추가(useradd + passwd)
- /etc/passwd : 로그인정보, UID , GID , HOME DIR, SHELL 
- /etc/shadow : password age < /etc/login.defs
- /etc/group  : 그룹정보
- /home/$USER : useradd -D의 옵션에 따라 홈디렉토리 생성 
- /var/spool/mail/$USER : useradd -D의 옵션에 따라 메일박스 생성 

# useradd user01 
# passwd user01
Changing password for user user01.
New UNIX password: (user01)
BAD PASSWORD: it is based on a dictionary word
Retype new UNIX password: (user01)
passwd: all authentication tokens updated successfully.

# grep user01 /etc/passwd
user01:x:1000:1000::/home/user01:/bin/bash
-> 사용자 UID : 1000
-> 사용자 GID : 1000
-> 사용자 홈디렉토리 : /home/user01
-> 로그인 쉘 : /bin/bash
	RHEL5에서는 500번부터 일반 사용자로 구성하였지만 RHEL7은 UID 1000부터 일반 계정으로 사용한다.

# grep user01 /etc/shadow
user01:$6$BGm87tLm$/WdAYx.pEYZ08Ov.d.nXDhp.4FRqI33VZeGp6lCWkQZC1zgSVquRx9Amyxu63NLf8hol3yPkFIkAiSwftFoba0:18352:0:99999:7:::
# grep user01 /etc/group
user01:x:1000:
# ls -ld /home/user01
drwx------ 3 user01 user01 4.0K Mar  1 04:55 /home/user01
-> 홈디렉토리의 퍼미션이 700(rwx------)이다. 
   (다른사용자는 /home/user01 디렉토리안에를 볼수 없다.)
# ls -l /var/spool/mail
fedora   root   rpc   user01

② user02 사용자 추가 및 정보 확인
# mkdir -p /users 
# useradd -d /users/user02 user02 
# passwd user02

Changing password for user user02.
New UNIX password: (user02 사용자의 암호 입력)
BAD PASSWORD: it is based on a dictionary word
Retype new UNIX password: (user02 사용자의 암호 입력)
passwd: all authentication tokens updated successfully.
# grep user02 /etc/passwd
user02:x:1001:1001::/users/user02:/bin/bash
# grep user02 /etc/shadow
user02:$6$Z6l4c1Oa$KS/Nu71nGfc5n2LDELsEGE.Ghwup7bm6TEs2so6yHIPMq0oJsEDs7W5yAa5PdPjKFN9q.X05FQN42H1vRuqXM1:18352:0:99999:7:::
# ls -ld /users/user02
drwx------. 5 user02 user02 128  3월 31 10:45 /users/user02
# ls /var/spool/mail
fedora   root   rpc   user01    user02
```

### [참고] skel 파일 구성후 유저 구성

```bash
# cd /etc
# cp -r skel/ admin.skel
# cd admin.skel/
# ls -a
.  ..  .bash_logout  .bash_profile  .bashrc  .mozilla
# echo "ADMIN ID" > file1.txt
# mkdir bin
# useradd -mk /etc/admin.skel admin1
# passwd admin1
# su - admin1
[admin1@server1 ~]$ ls
bin  file1.txt
[admin1@server1 ~]$ cat file1.txt 
ADMIN ID
[admin1@server1 ~]$

# seq -w 1 100 < 자리수 채워주면서 숫자
```

## (2-2) usermod 명령어

[명령어 형식]

```bash
# usermod [options] LOGIN

Options:
-a, --append  append the user to the supplemental GROUPS(use only with -G)
-c, --comment COMMENT  new value of the GECOS field
-d, --home HOME_DIR  new home directory for the user account
-e, --expiredate EXPIRE_DATE  set account expiration date to EXPIRE_DATE
-f, --inactive INACTIVE  set password inactive after expiration to INACTIVE
-g, --gid GROUP  force use GROUP as new primary group
-G, --groups GROUPS  new list of supplementary GROUPS
-h, --help  display this help message and exit
-l, --login NEW_LOGIN  new value of the login name
-L, --lock  lock the user account
-m, --move-home  move contents of the home directory to the new location (use only 
with -d)
-o, --non-unique  allow using duplicate (non-unique) UID
-p, --password PASSWORD  use encrypted password for the new password
-s, --shell SHELL  new login shell for the user account
-u, --uid UID  new UID for the user account
-U, --unlock  unlock the user account
-Z, --selinux-user  new selinux user mapping for the user account

(/etc/passwd)
user01:x:1000:1000::/home/user01:/bin/bash
-l        -u  -g  -c     -d           -s

(명령어 사용예)
# usermod -u 2000 user01
# usermod -s /bin/ksh user01
# usermod -g 10 -c "Test Group" user01 
# usermod -l user03 -d /home/user03 -m user01  (-l : login name, -m : move directory)
```

### [EX1] user01 사용자 정보 변경

```bash
# grep user01 /etc/passwd
user01:x:1000:1000::/home/user01:/bin/bash
# cat /etc/passwd | awk -F : '{print $3}'| grep -w 2000 < UID가 2000번인 계정을 확인
< 2000번으로 설정된 계정이 없음을 확인
# usermod -u 2000 user01
# grep user01 /etc/passwd
user01:x:**2000**:1000::/home/user01:/bin/bash
# id user01
uid=2000(user01) gid=1000(user01) groups=1000(user01)
# yum -y install ksh
# usermod -s /bin/ksh user01 
chsh 명령어를 사용하도록 가이드
# dnf -y install util-linux-user

# grep user01 /etc/passwd
user01:x:2000:1000::/home/user01:**/bin/ksh**
# usermod -l user03 -d /home/user03 -m user01  (-l : Login name, -d : Directory, -m : Move directory)
# grep user03 /etc/passwd
user03:x:2000:1000::/home/user03:/bin/ksh
# ls –l /home
drwx------ 3 fedora fedora 4.0K Feb  3 02:08 fedora
drwx------ 3 user03 user01 4.0K Mar  1 05:21 user03
-> 사용자 홈디렉토리가 /home/user03으로 변경 되었다.
-> 홈디렉토리의 소유권은 user03으로 변경 되었지만, 그룹권은 user01 그대로이다.
-> 따라서, 리눅스에서 사용자를 변경한다면, 보통은 다음과 같은 절차여야 한다.
(user01 -> user03 사용자 변경)
# groupadd -g 2000 user03  < 변경할 UID를 미리 맞춰서 설정
# usermod -l user03 -g user03 -d /home/user03 -m user03
# groupdel user01 < 삭제하는 경우 파일이 구성되어 있다면 바로 삭제가 불가능 * 
groupdel: 'user03' 사용자의 주요 그룹을 제거할 수 없습니다
# id user03
uid=2000(user03) gid=1000(user01) groups=1000(user01)
# usermod -g 2000 user03
# id user03
uid=2000(user03) gid=2000(user03) groups=2000(user03)
# groupdel user01
```

## (2-3) userdel 명령어

[명령어 형식]

```bash
# userdel [options] LOGIN

Options:
-f, --force  force removal of files, even if not owned by user
-h, --help  display this help message and exit
-r, --remove  remove home directory and mail spool
```

[명령어 사용예]

```bash
# userdel user01       /* /etc/passwd, /etc/shadow 정보 삭제 */
# userdel -r user01    /* /etc/passwd, /etc/shadow + Home Directory 삭제 */

```

### [EX] 사용자 삭제 실습

```bash
① user02 사용자 삭제 및 확인

■ 사용자 삭제(userdel or userdel –r)
(userdel)
- /etc/passwd
- /etc/shadow
- /etc/group
(userdel –r)
- /etc/passwd
- /etc/shadow
- /etc/group
- /home/$USER
- /var/spool/mail/$USER

# userdel user02 
# grep user02 /etc/passwd 
#

# ls -l /users
drwx------ 3 502 502 4.0K Mar  1 04:58 user02
-> 파일의 소유권/그룹권 부분이 숫자로 표시난다. 
-> 이것은 사용자나 그룹 정보(/etc/passwd, /etc/shadow)가 없는데 사용자의 파일이 존재하는 경우이다.

# rm -rf /users/user02 
# rm -rf /var/spool/mail/user02

② user03 사용자 삭제
# userdel -r user03 
#

-r : 현재 완전 삭제를 해도 아무런 문제가 없는 경우 삭제는 하지만 사용자의 완전삭제에 유예를 둬야 하는 경우
```
